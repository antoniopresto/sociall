import { mutationWithClientMutationId } from 'graphql-relay';

export default mutationWithClientMutationId({
  name: 'FacebookLogin',
  inputFields: {},
  mutateAndGetPayload: () => {},
  outputFields: {},
});
